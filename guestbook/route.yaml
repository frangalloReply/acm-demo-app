apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: guestbook
spec:
  host: guestbook.apps.aws-1.aws-poc-clusters.mcatest.org
  port:
    targetPort: 80
  to:
    kind: Service
    name: guestbook
    weight: 100
